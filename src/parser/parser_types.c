#include "cub3d.h"

void
	ft_types(int fd, t_map *map)
{
	(void)fd;
	(void)map;
	// INIT TYPES

	//	WHILE GNL
	//		SPLIT
	//		IF (SPLIT == PAIR NUMBER || SPLIT == EMPTY)
	//			IF (LINE != VALID) FAIL
	//			PARSE
	//			IF (TYPES == END) RETURN
	//		ELSE FAIL
	//	FAIL

	// PARSE ARGS
}
